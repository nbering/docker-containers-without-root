---
version: "3.7"
services:
  control:
    build:
      context: .
      dockerfile: samples/01_control/Dockerfile
  dockerfile_user:
    build:
      context: .
      dockerfile: samples/02_dockerfile_user/Dockerfile
